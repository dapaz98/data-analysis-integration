Task 1: Organize functions with colleague
	We have 1 month to delivery the project.
	Analysis the question to ansewer with ETL.

Task 2: Method the division of task. 

GOALS: Award the prize and delivery the in 2 weeks.


select b.dept_no, b.dept_name, count(emp_no) as count_emp_no
from curr_dept_emp as a, departments as b
where a.dept_no = b.dept_no
group by b.dept_no, b.dept_name
having count_emp_no >= 40
order by count_emp_no desc;
